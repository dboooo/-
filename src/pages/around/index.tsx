import { Component, PropsWithChildren, ReactNode } from "react"
import { View } from "@tarojs/components"

export default class Around extends Component<PropsWithChildren> {
  render(): ReactNode {
      return (
        <>
          <View>124124</View>
        </>
      )
  }
}
